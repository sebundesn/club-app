# 🛠 共同開発ルール

---

## 1. コミットメッセージの規則

コミットメッセージは **`種類: 変更内容の概要`** の形式で記述
後から履歴を追いやすくするため、1つのコミットには1つの意図だけを含めるようにこまめにコミット

### 📌 種類一覧 [例付き]

* **`feat:`（新機能の追加）**
  * `feat: 会計ログにCSVエクスポート機能を追加`
  * `feat(auth): パスワードリセット機能のバックエンドAPI実装`

* **`fix:`（バグ・不具合の修正）**
  * `fix: タイムゾーンのズレによりカレンダーの日付が1日ずれる問題を修正`

* **`docs:`（ドキュメントの修正）**
  * `docs: コントローラー層のソースコードにGoDocのコメントを追加`
  * `docs: READMEの起動手順を修正`

* **`refactor:`（コードの整理）**
  * `refactor: 重複していたSQLクエリを共通メソッドに統合`

* **`chore:`（その他の雑務・設定変更）**
  * `chore: 不要になったログ出力コード（fmt.Println等）の削除`
  * `chore: データベースのマイグレーションファイルを生成`

---

## 2. ブランチの命名規則

新しく作業を始めるときは、必ず `main` ブランチから新しい作業ブランチを切る。
名前は **`種類/詳細な作業内容`** の形式にする

### 📌 種類一覧
* `feature/` : 新機能の追加、画面や機能の実装
* `fix/`: バグや不具合の修正
* `docs/` : ドキュメントやコメントのみの修正
* `style/` : コードの意味に変更を与えない修正（インデントやフォーマットの調整）
* `refactor/` : リファクタリング（機能は変えずにコードを綺麗にする）
* `test/` : テストコードの追加・修正
* `chore/` : ライブラリのアップデートや設定ファイルの変更

### ✍️ 具体的な命名例
* `feature/login-page` （ログイン画面の実装）
* `feature/user-profile-api` （ユーザープロフィールのAPI実装）
* `fix/signup-validation` （新規登録のバリデーションバグ修正）
* `refactor/database-connection` （DB接続処理のリファクタリング）

---

## 3. main ブランチにマージするまでの流れ

### ① 作業ブランチの作成と実装
必ず`main` からブランチを切り、作業を行う
```bash
git checkout main
git pull origin main
git checkout -b feature/作業内容
```

### ② プルリクエスト（PR）の作成
GitHub上で「自分の書いたコードをメインのコードに合流させてください」と Pull Request を送る
記載すること: 変更内容の概要、確認してほしいポイント、動作確認の結果など

### ③ コードレビュー

### ④ マージ（合流）とブランチの削除
レビューでOKをもらったら、 main ブランチへマージ
マージが完了したら、役割の終わった作業用ブランチは削除